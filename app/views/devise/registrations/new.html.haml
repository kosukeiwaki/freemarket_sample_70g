.signup
  .signup__container
    .signup__container--title
      %h2 新規登録
      = render "devise/shared/links"
    .signup__container--form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .field
          .field-label
            = f.label :ニックネーム
          .field-input
            = f.text_field :nickname, autofocus: true,placeholder: "例:フリマたかし"

        .field
          .field-label
            = f.label :メールアドレス
          .field-input
            = f.email_field :email,placeholder: "PC・携帯どちらでも可"

        .field
          .field-label
            = f.label :パスワード
          .field-input
            = f.password_field :password, autocomplete: "off", placeholder: "7文字以上の半角英数字"

        .field
          .field-label
            = f.label :パスワード確認
          .field-input
            = f.password_field :password_confirmation, autocomplete: "off"

        .field
          .field-label
            = f.label :苗字
          .field-input
            = f.text_field :lastname, autofocus: true

        .field
          .field-label
            = f.label :名前
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :生年月日
          .field-input
            = f.text_field :birthday, autofocus: true

        %p 送付先氏名
        .field
          .field-label
            = f.label :苗字
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :名前
          .field-input
            = f.text_field :firstname, autofocus: true

        %p 送付先住所
        .field
          .field-label
            = f.label :郵便番号
          .field-input
            = f.text_field :nickname, autofocus: true

        .field
          .field-label
            = f.label :都道府県
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :市区町村
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :番地
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :マンション名、部屋番号
          .field-input
            = f.text_field :firstname, autofocus: true

        .field
          .field-label
            = f.label :お届け先電話番号
          .field-input
            = f.text_field :firstname, autofocus: true


        .actions
          = f.submit "登録", class: 'btn'
